# Copy this file to `.env` once a backend exists and set the real URL.
# Until then, the app runs fine without this file — auth requests
# will fail to reach a server and the app falls back to a local
# "demo mode" (see src/context/AuthContext.jsx).

VITE_API_BASE_URL=http://localhost:5000/api
